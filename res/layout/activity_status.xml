<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".StatusActivity" >

    <Button
        android:id="@+id/status_send"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/status_message"
        android:text="@string/status_send_text" />

    <TextView
        android:id="@+id/status_remaining_characters"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/status_send"
        android:text="@string/status_remaining_characters_pendent"
        android:layout_alignRight="@+id/status_message"
        android:textIsSelectable="true" />

    <EditText
        android:id="@+id/status_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/status_send"
        android:layout_centerHorizontal="true"
        android:ems="10"
        android:inputType="text" >
    </EditText>

</RelativeLayout>